// const must have a type
//cannot have mut 
// Declared with capital letters e.g. const Y: i32 = 10;
// Can declare constants in any scope, including the global scope (outside the main function)
fn main() {
    println!("The value of Y is {}", Y);
}
const Y: i32 = 10;